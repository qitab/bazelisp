# Description: mixalot, mixer for ALSA/libao

licenses(["unencumbered"]) # MIT

load("@lisp__bazel//:bazel/rules.bzl", "lisp_library", "lisp_test")

# TODO: add a cffi_grovel rule, add support for all those libraries, build.
